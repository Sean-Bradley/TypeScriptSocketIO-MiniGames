type GameState = {
    id: number
    title: string
    logo: string
    gamePhase: number
    gameClock: number
    winners: string[]
    winnersCalculated: boolean
    duration: number    
    result: number
}